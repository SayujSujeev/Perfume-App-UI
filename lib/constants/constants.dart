import 'package:flutter/material.dart';

// Padding
const double appPadding = 20.0;

//Colors used in this app
const primaryColor = Colors.black;
const secondaryColor = Colors.white;
const textColor = Color(0xFFFFC400);